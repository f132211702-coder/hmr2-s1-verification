# work

可丟棄或隔離的工作區：草稿、暫存輸出、個別 agent／使用者的中間產物。不會被視為 shared truth，不需要通過審查即可寫入，但也不應被其他文件當成證據來源直接引用。

（`4D-Humans/data/{test_out,test_out_refined,test01_out,s1_out}/` 目前是手動 demo 測試輸出，性質上等同這裡的內容，暫時留在原地未搬動，避免打亂腳本的相對路徑假設；已在 `4D-Humans/.gitignore` 排除，不會進 git。）
