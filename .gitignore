# Local dev venv — not portable, never commit
/hmr2_venv/

# OS
.DS_Store
**/.DS_Store

# Python
__pycache__/
*.py[cod]
*.egg-info/

# Downloaded checkpoints, run outputs, sandbox scratch (from the vendored
# 4D-Humans release's own .gitignore, carried over since it no longer has
# its own nested .git)
/results/
/logs*/
/sandbox/
/discord_sandbox/
/demo_out/
/outputs/
/checkpoints/
/data/
*.lock
*.pt
*.npy
*.tar
*.tar.gz
